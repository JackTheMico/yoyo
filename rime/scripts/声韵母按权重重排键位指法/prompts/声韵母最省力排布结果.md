# 声韵母最省力排布结果

## 本轮完整字词表（优化前先核对）

> 本轮实际选出 350 条两码字词记录、350 个不同字词，并非 360 条。缺少的 10 条来自空格：sE、fB、fJ、fL、xE。
> 下列记录严格按权重从高到低排列；`【A位】` 表示该字词在本轮排布后的新编码使用指法 A。

### 本轮 A 指法位置

共 30 条记录：

- `qA`：没有[词]（10,148,614）、吃[字]（8,181,885）
- `wA`：我[字]（41,082,993）、我们[词]（9,668,711）
- `eA`：是[字]（60,632,202）、上[字]（17,672,983）
- `rA`：不[字]（32,331,838）、被[字]（10,735,161）
- `tA`：小[字]（9,678,711）、下[字]（7,533,527）
- `aA`：中[字]（22,094,930）、你[字]（20,296,787）
- `sA`：和[字]（46,836,227）、很[字]（21,847,963）
- `dA`：的[字]（92,123,018）、人[字]（22,310,236）
- `fA`：有[字]（33,869,044）、一[字]（27,356,423）
- `gA`：在[字]（57,072,466）、去[字]（12,095,319）
- `zA`：他[字]（14,507,163）、她[字]（6,159,034）
- `xA`：更[字]（10,117,320）、个[字]（9,304,535）
- `cA`：了[字]（59,562,863）、来[字]（12,023,272）
- `vA`：就[字]（26,406,172）、可以[词]（20,283,603）
- `bA`：而[字]（9,619,021）、所以[词]（4,964,151）

### 权重前 105 中未进入 A 的字词

共 76 条，按全局权重排名从高到低排列：

- 全局第 12：也[字] — 权重 22,136,145；原编码 `vL`；本轮新编码 `fF`
- 全局第 17：都[字] — 权重 18,779,556；原编码 `aK`；本轮新编码 `dC`
- 全局第 19：会[字] — 权重 17,543,962；原编码 `cF`；本轮新编码 `sL`
- 全局第 20：这[字] — 权重 16,995,923；原编码 `dL`；本轮新编码 `aF`
- 全局第 21：一个[词] — 权重 16,335,779；原编码 `vE`；本轮新编码 `fE`
- 全局第 22：好[字] — 权重 16,120,003；原编码 `cD`；本轮新编码 `sD`
- 全局第 23：到[字] — 权重 14,948,468；原编码 `aD`；本轮新编码 `dD`
- 全局第 24：对[字] — 权重 14,880,376；原编码 `aF`；本轮新编码 `dL`
- 全局第 25：大[字] — 权重 14,749,051；原编码 `aG`；本轮新编码 `dG`
- 全局第 27：等[字] — 权重 14,146,876；原编码 `aB`；本轮新编码 `dK`
- 全局第 28：要[字] — 权重 13,943,971；原编码 `vD`；本轮新编码 `fD`
- 全局第 29：多[字] — 权重 13,521,988；原编码 `aI`；本轮新编码 `dB`
- 全局第 30：还[字] — 权重 12,829,849；原编码 `cJ`；本轮新编码 `sJ`
- 全局第 31：年[字] — 权重 12,349,408；原编码 `dD`；本轮新编码 `aD`
- 全局第 32：说[字] — 权重 12,236,910；原编码 `wI`；本轮新编码 `eB`
- 全局第 35：与[字] — 权重 11,869,898；原编码 `vK`；本轮新编码 `fC`
- 全局第 36：能[字] — 权重 11,093,126；原编码 `dB`；本轮新编码 `aK`
- 全局第 37：自己[词] — 权重 10,928,418；原编码 `tE`；本轮新编码 `gE`
- 全局第 38：后[字] — 权重 10,787,364；原编码 `cK`；本轮新编码 `sC`
- 全局第 40：时[字] — 权重 10,435,817；原编码 `wE`；本轮新编码 `eE`
- 全局第 41：以[字] — 权重 10,294,298；原编码 `vE`；本轮新编码 `fE`
- 全局第 44：将[字] — 权重 9,976,310；原编码 `xG`；本轮新编码 `vG`
- 全局第 45：但[字] — 权重 9,868,389；原编码 `aH`；本轮新编码 `dH`
- 全局第 49：看[字] — 权重 9,354,052；原编码 `xH`；本轮新编码 `vH`
- 全局第 51：这个[词] — 权重 9,010,578；原编码 `dL`；本轮新编码 `aF`
- 全局第 52：问题[词] — 权重 8,523,902；原编码 `gI`；本轮新编码 `wB`
- 全局第 53：为[字] — 权重 8,517,567；原编码 `gF`；本轮新编码 `wL`
- 全局第 54：用[字] — 权重 8,356,257；原编码 `vA`；本轮新编码 `fI`
- 全局第 55：过[字] — 权重 8,319,337；原编码 `zI`；本轮新编码 `xB`
- 全局第 56：最[字] — 权重 8,249,156；原编码 `tF`；本轮新编码 `gL`
- 全局第 58：中国[词] — 权重 7,864,575；原编码 `dA`；本轮新编码 `aI`
- 全局第 59：并[字] — 权重 7,754,066；原编码 `qL`；本轮新编码 `rF`
- 全局第 60：日[字] — 权重 7,707,449；原编码 `aE`；本轮新编码 `dE`
- 全局第 61：不错[词] — 权重 7,572,179；原编码 `qK`；本轮新编码 `rC`
- 全局第 62：什么[词] — 权重 7,548,615；原编码 `wI`；本轮新编码 `eB`
- 全局第 63：可[字] — 权重 7,546,661；原编码 `xL`；本轮新编码 `vF`
- 全局第 65：做[字] — 权重 7,527,841；原编码 `tI`；本轮新编码 `gB`
- 全局第 66：于[字] — 权重 7,480,438；原编码 `vK`；本轮新编码 `fC`
- 全局第 67：进行[词] — 权重 7,405,210；原编码 `xH`；本轮新编码 `vH`
- 全局第 68：从[字] — 权重 7,402,629；原编码 `gA`；本轮新编码 `wI`
- 全局第 69：还是[词] — 权重 7,365,741；原编码 `cJ`；本轮新编码 `sJ`
- 全局第 70：使用[词] — 权重 7,342,956；原编码 `wE`；本轮新编码 `eE`
- 全局第 71：数据[词] — 权重 7,209,667；原编码 `wK`；本轮新编码 `eC`
- 全局第 72：没[字] — 权重 7,198,338；原编码 `eF`；本轮新编码 `qL`
- 全局第 73：需要[词] — 权重 7,192,580；原编码 `rK`；本轮新编码 `tC`
- 全局第 74：新[字] — 权重 7,075,049；原编码 `rH`；本轮新编码 `tH`
- 全局第 75：里[字] — 权重 7,046,887；原编码 `fE`；本轮新编码 `cE`
- 全局第 76：给[字] — 权重 6,985,613；原编码 `zF`；本轮新编码 `xL`
- 全局第 77：高[字] — 权重 6,825,553；原编码 `zD`；本轮新编码 `xD`
- 全局第 78：公司[词] — 权重 6,807,455；原编码 `zA`；本轮新编码 `xI`
- 全局第 79：想[字] — 权重 6,774,355；原编码 `rG`；本轮新编码 `tG`
- 全局第 80：地[字] — 权重 6,770,277；原编码 `aE`；本轮新编码 `dE`
- 全局第 81：学习[词] — 权重 6,676,248；原编码 `rG`；本轮新编码 `tG`
- 全局第 82：但是[词] — 权重 6,617,519；原编码 `aH`；本轮新编码 `dH`
- 全局第 83：都是[词] — 权重 6,555,540；原编码 `aK`；本轮新编码 `dC`
- 全局第 84：点[字] — 权重 6,473,870；原编码 `aD`；本轮新编码 `dD`
- 全局第 85：出[字] — 权重 6,387,187；原编码 `eK`；本轮新编码 `qC`
- 全局第 86：喜欢[词] — 权重 6,240,180；原编码 `rE`；本轮新编码 `tE`
- 全局第 87：或[字] — 权重 6,169,701；原编码 `cI`；本轮新编码 `sB`
- 全局第 88：因为[词] — 权重 6,165,763；原编码 `vH`；本轮新编码 `fH`
- 全局第 90：之[字] — 权重 6,062,185；原编码 `dE`；本轮新编码 `aE`
- 全局第 91：技术[词] — 权重 6,030,657；原编码 `xE`；本轮新编码 `vE`
- 全局第 92：太[字] — 权重 5,983,780；原编码 `sJ`；本轮新编码 `zJ`
- 全局第 93：本[字] — 权重 5,789,736；原编码 `qI`；本轮新编码 `rB`
- 全局第 94：年月[词] — 权重 5,768,321；原编码 `dD`；本轮新编码 `aD`
- 全局第 95：他们[词] — 权重 5,674,755；原编码 `sG`；本轮新编码 `zG`
- 全局第 96：其[字] — 权重 5,547,992；原编码 `tE`；本轮新编码 `gE`
- 全局第 97：把[字] — 权重 5,538,031；原编码 `qG`；本轮新编码 `rG`
- 全局第 98：很多[词] — 权重 5,401,033；原编码 `cI`；本轮新编码 `sB`
- 全局第 99：比较[词] — 权重 5,311,941；原编码 `qE`；本轮新编码 `rE`
- 全局第 100：感觉[词] — 权重 5,245,417；原编码 `zH`；本轮新编码 `xH`
- 全局第 101：不是[词] — 权重 5,186,652；原编码 `qK`；本轮新编码 `rC`
- 全局第 102：通过[词] — 权重 5,140,993；原编码 `sA`；本轮新编码 `zI`
- 全局第 103：选择[词] — 权重 5,132,905；原编码 `rB`；本轮新编码 `tK`
- 全局第 104：比[字] — 权重 5,115,851；原编码 `qE`；本轮新编码 `rE`
- 全局第 105：再[字] — 权重 5,113,828；原编码 `tJ`；本轮新编码 `gJ`

### 全部两码记录（权重降序）

1. 的[字] **【A位】** — 权重 92,123,018；原编码 `aL`；本轮新编码 `dA`
2. 是[字] **【A位】** — 权重 60,632,202；原编码 `wE`；本轮新编码 `eA`
3. 了[字] **【A位】** — 权重 59,562,863；原编码 `fL`；本轮新编码 `cA`
4. 在[字] **【A位】** — 权重 57,072,466；原编码 `tJ`；本轮新编码 `gA`
5. 和[字] **【A位】** — 权重 46,836,227；原编码 `cL`；本轮新编码 `sA`
6. 我[字] **【A位】** — 权重 41,082,993；原编码 `gA`；本轮新编码 `wA`
7. 有[字] **【A位】** — 权重 33,869,044；原编码 `vK`；本轮新编码 `fA`
8. 不[字] **【A位】** — 权重 32,331,838；原编码 `qK`；本轮新编码 `rA`
9. 一[字] **【A位】** — 权重 27,356,423；原编码 `vE`；本轮新编码 `fA`
10. 就[字] **【A位】** — 权重 26,406,172；原编码 `xJ`；本轮新编码 `vA`
11. 人[字] **【A位】** — 权重 22,310,236；原编码 `aI`；本轮新编码 `dA`
12. 也[字] — 权重 22,136,145；原编码 `vL`；本轮新编码 `fF`
13. 中[字] **【A位】** — 权重 22,094,930；原编码 `dA`；本轮新编码 `aA`
14. 很[字] **【A位】** — 权重 21,847,963；原编码 `cI`；本轮新编码 `sA`
15. 你[字] **【A位】** — 权重 20,296,787；原编码 `dE`；本轮新编码 `aA`
16. 可以[词] **【A位】** — 权重 20,283,603；原编码 `xL`；本轮新编码 `vA`
17. 都[字] — 权重 18,779,556；原编码 `aK`；本轮新编码 `dC`
18. 上[字] **【A位】** — 权重 17,672,983；原编码 `wC`；本轮新编码 `eA`
19. 会[字] — 权重 17,543,962；原编码 `cF`；本轮新编码 `sL`
20. 这[字] — 权重 16,995,923；原编码 `dL`；本轮新编码 `aF`
21. 一个[词] — 权重 16,335,779；原编码 `vE`；本轮新编码 `fE`
22. 好[字] — 权重 16,120,003；原编码 `cD`；本轮新编码 `sD`
23. 到[字] — 权重 14,948,468；原编码 `aD`；本轮新编码 `dD`
24. 对[字] — 权重 14,880,376；原编码 `aF`；本轮新编码 `dL`
25. 大[字] — 权重 14,749,051；原编码 `aG`；本轮新编码 `dG`
26. 他[字] **【A位】** — 权重 14,507,163；原编码 `sG`；本轮新编码 `zA`
27. 等[字] — 权重 14,146,876；原编码 `aB`；本轮新编码 `dK`
28. 要[字] — 权重 13,943,971；原编码 `vD`；本轮新编码 `fD`
29. 多[字] — 权重 13,521,988；原编码 `aI`；本轮新编码 `dB`
30. 还[字] — 权重 12,829,849；原编码 `cJ`；本轮新编码 `sJ`
31. 年[字] — 权重 12,349,408；原编码 `dD`；本轮新编码 `aD`
32. 说[字] — 权重 12,236,910；原编码 `wI`；本轮新编码 `eB`
33. 去[字] **【A位】** — 权重 12,095,319；原编码 `tK`；本轮新编码 `gA`
34. 来[字] **【A位】** — 权重 12,023,272；原编码 `fJ`；本轮新编码 `cA`
35. 与[字] — 权重 11,869,898；原编码 `vK`；本轮新编码 `fC`
36. 能[字] — 权重 11,093,126；原编码 `dB`；本轮新编码 `aK`
37. 自己[词] — 权重 10,928,418；原编码 `tE`；本轮新编码 `gE`
38. 后[字] — 权重 10,787,364；原编码 `cK`；本轮新编码 `sC`
39. 被[字] **【A位】** — 权重 10,735,161；原编码 `qF`；本轮新编码 `rA`
40. 时[字] — 权重 10,435,817；原编码 `wE`；本轮新编码 `eE`
41. 以[字] — 权重 10,294,298；原编码 `vE`；本轮新编码 `fE`
42. 没有[词] **【A位】** — 权重 10,148,614；原编码 `eF`；本轮新编码 `qA`
43. 更[字] **【A位】** — 权重 10,117,320；原编码 `zB`；本轮新编码 `xA`
44. 将[字] — 权重 9,976,310；原编码 `xG`；本轮新编码 `vG`
45. 但[字] — 权重 9,868,389；原编码 `aH`；本轮新编码 `dH`
46. 小[字] **【A位】** — 权重 9,678,711；原编码 `rA`；本轮新编码 `tA`
47. 我们[词] **【A位】** — 权重 9,668,711；原编码 `gA`；本轮新编码 `wA`
48. 而[字] **【A位】** — 权重 9,619,021；原编码 `bB`；本轮新编码 `bA`
49. 看[字] — 权重 9,354,052；原编码 `xH`；本轮新编码 `vH`
50. 个[字] **【A位】** — 权重 9,304,535；原编码 `zL`；本轮新编码 `xA`
51. 这个[词] — 权重 9,010,578；原编码 `dL`；本轮新编码 `aF`
52. 问题[词] — 权重 8,523,902；原编码 `gI`；本轮新编码 `wB`
53. 为[字] — 权重 8,517,567；原编码 `gF`；本轮新编码 `wL`
54. 用[字] — 权重 8,356,257；原编码 `vA`；本轮新编码 `fI`
55. 过[字] — 权重 8,319,337；原编码 `zI`；本轮新编码 `xB`
56. 最[字] — 权重 8,249,156；原编码 `tF`；本轮新编码 `gL`
57. 吃[字] **【A位】** — 权重 8,181,885；原编码 `eE`；本轮新编码 `qA`
58. 中国[词] — 权重 7,864,575；原编码 `dA`；本轮新编码 `aI`
59. 并[字] — 权重 7,754,066；原编码 `qL`；本轮新编码 `rF`
60. 日[字] — 权重 7,707,449；原编码 `aE`；本轮新编码 `dE`
61. 不错[词] — 权重 7,572,179；原编码 `qK`；本轮新编码 `rC`
62. 什么[词] — 权重 7,548,615；原编码 `wI`；本轮新编码 `eB`
63. 可[字] — 权重 7,546,661；原编码 `xL`；本轮新编码 `vF`
64. 下[字] **【A位】** — 权重 7,533,527；原编码 `rC`；本轮新编码 `tA`
65. 做[字] — 权重 7,527,841；原编码 `tI`；本轮新编码 `gB`
66. 于[字] — 权重 7,480,438；原编码 `vK`；本轮新编码 `fC`
67. 进行[词] — 权重 7,405,210；原编码 `xH`；本轮新编码 `vH`
68. 从[字] — 权重 7,402,629；原编码 `gA`；本轮新编码 `wI`
69. 还是[词] — 权重 7,365,741；原编码 `cJ`；本轮新编码 `sJ`
70. 使用[词] — 权重 7,342,956；原编码 `wE`；本轮新编码 `eE`
71. 数据[词] — 权重 7,209,667；原编码 `wK`；本轮新编码 `eC`
72. 没[字] — 权重 7,198,338；原编码 `eF`；本轮新编码 `qL`
73. 需要[词] — 权重 7,192,580；原编码 `rK`；本轮新编码 `tC`
74. 新[字] — 权重 7,075,049；原编码 `rH`；本轮新编码 `tH`
75. 里[字] — 权重 7,046,887；原编码 `fE`；本轮新编码 `cE`
76. 给[字] — 权重 6,985,613；原编码 `zF`；本轮新编码 `xL`
77. 高[字] — 权重 6,825,553；原编码 `zD`；本轮新编码 `xD`
78. 公司[词] — 权重 6,807,455；原编码 `zA`；本轮新编码 `xI`
79. 想[字] — 权重 6,774,355；原编码 `rG`；本轮新编码 `tG`
80. 地[字] — 权重 6,770,277；原编码 `aE`；本轮新编码 `dE`
81. 学习[词] — 权重 6,676,248；原编码 `rG`；本轮新编码 `tG`
82. 但是[词] — 权重 6,617,519；原编码 `aH`；本轮新编码 `dH`
83. 都是[词] — 权重 6,555,540；原编码 `aK`；本轮新编码 `dC`
84. 点[字] — 权重 6,473,870；原编码 `aD`；本轮新编码 `dD`
85. 出[字] — 权重 6,387,187；原编码 `eK`；本轮新编码 `qC`
86. 喜欢[词] — 权重 6,240,180；原编码 `rE`；本轮新编码 `tE`
87. 或[字] — 权重 6,169,701；原编码 `cI`；本轮新编码 `sB`
88. 因为[词] — 权重 6,165,763；原编码 `vH`；本轮新编码 `fH`
89. 她[字] **【A位】** — 权重 6,159,034；原编码 `sG`；本轮新编码 `zA`
90. 之[字] — 权重 6,062,185；原编码 `dE`；本轮新编码 `aE`
91. 技术[词] — 权重 6,030,657；原编码 `xE`；本轮新编码 `vE`
92. 太[字] — 权重 5,983,780；原编码 `sJ`；本轮新编码 `zJ`
93. 本[字] — 权重 5,789,736；原编码 `qI`；本轮新编码 `rB`
94. 年月[词] — 权重 5,768,321；原编码 `dD`；本轮新编码 `aD`
95. 他们[词] — 权重 5,674,755；原编码 `sG`；本轮新编码 `zG`
96. 其[字] — 权重 5,547,992；原编码 `tE`；本轮新编码 `gE`
97. 把[字] — 权重 5,538,031；原编码 `qG`；本轮新编码 `rG`
98. 很多[词] — 权重 5,401,033；原编码 `cI`；本轮新编码 `sB`
99. 比较[词] — 权重 5,311,941；原编码 `qE`；本轮新编码 `rE`
100. 感觉[词] — 权重 5,245,417；原编码 `zH`；本轮新编码 `xH`
101. 不是[词] — 权重 5,186,652；原编码 `qK`；本轮新编码 `rC`
102. 通过[词] — 权重 5,140,993；原编码 `sA`；本轮新编码 `zI`
103. 选择[词] — 权重 5,132,905；原编码 `rB`；本轮新编码 `tK`
104. 比[字] — 权重 5,115,851；原编码 `qE`；本轮新编码 `rE`
105. 再[字] — 权重 5,113,828；原编码 `tJ`；本轮新编码 `gJ`
106. 前[字] — 权重 5,084,175；原编码 `tD`；本轮新编码 `gD`
107. 现在[词] — 权重 5,066,698；原编码 `rD`；本轮新编码 `tD`
108. 买[字] — 权重 4,978,201；原编码 `eJ`；本轮新编码 `qJ`
109. 非常[词] — 权重 4,964,668；原编码 `rF`；本轮新编码 `tL`
110. 所以[词] **【A位】** — 权重 4,964,151；原编码 `bI`；本轮新编码 `bA`
111. 也是[词] — 权重 4,901,548；原编码 `vL`；本轮新编码 `fF`
112. 那[字] — 权重 4,881,987；原编码 `dG`；本轮新编码 `aG`
113. 模型[词] — 权重 4,841,834；原编码 `eA`；本轮新编码 `qI`
114. 该[字] — 权重 4,811,567；原编码 `zJ`；本轮新编码 `xJ`
115. 用户[词] — 权重 4,809,862；原编码 `vA`；本轮新编码 `fI`
116. 啊[字] — 权重 4,806,059；原编码 `bG`；本轮新编码 `bG`
117. 吧[字] — 权重 4,794,757；原编码 `qG`；本轮新编码 `rG`
118. 书[字] — 权重 4,751,858；原编码 `wK`；本轮新编码 `eC`
119. 成[字] — 权重 4,742,894；原编码 `eB`；本轮新编码 `qK`
120. 工作[词] — 权重 4,668,059；原编码 `zA`；本轮新编码 `xI`
121. 服务[词] — 权重 4,637,118；原编码 `rK`；本轮新编码 `tC`
122. 可能[词] — 权重 4,618,850；原编码 `xL`；本轮新编码 `vF`
123. 得[字] — 权重 4,569,032；原编码 `aL`；本轮新编码 `dF`
124. 所[字] — 权重 4,498,811；原编码 `bI`；本轮新编码 `bB`
125. 觉得[词] — 权重 4,434,973；原编码 `xG`；本轮新编码 `vG`
126. 元[字] — 权重 4,430,163；原编码 `vB`；本轮新编码 `fK`
127. 推荐[词] — 权重 4,383,297；原编码 `sF`；本轮新编码 `zL`
128. 们[字] — 权重 4,360,245；原编码 `eI`；本轮新编码 `qB`
129. 才[字] — 权重 4,255,882；原编码 `gJ`；本轮新编码 `wJ`
130. 味道[词] — 权重 4,198,857；原编码 `gF`；本轮新编码 `wL`
131. 开始[词] — 权重 4,193,386；原编码 `xJ`；本轮新编码 `vJ`
132. 及[字] — 权重 4,184,384；原编码 `xE`；本轮新编码 `vE`
133. 内[字] — 权重 4,146,610；原编码 `dF`；本轮新编码 `aL`
134. 知道[词] — 权重 4,137,449；原编码 `dE`；本轮新编码 `aE`
135. 分析[词] — 权重 4,133,371；原编码 `rI`；本轮新编码 `tB`
136. 请[字] — 权重 4,064,912；原编码 `tL`；本轮新编码 `gF`
137. 美国[词] — 权重 4,046,037；原编码 `eF`；本轮新编码 `qL`
138. 包括[词] — 权重 3,953,579；原编码 `qD`；本轮新编码 `rD`
139. 产品[词] — 权重 3,933,476；原编码 `eH`；本轮新编码 `qH`
140. 系统[词] — 权重 3,923,954；原编码 `rE`；本轮新编码 `tE`
141. 大家[词] — 权重 3,911,639；原编码 `aG`；本轮新编码 `dG`
142. 提供[词] — 权重 3,904,784；原编码 `sE`；本轮新编码 `zE`
143. 月[字] — 权重 3,827,112；原编码 `vG`；本轮新编码 `fG`
144. 三[字] — 权重 3,822,743；原编码 `bH`；本轮新编码 `bH`
145. 性[字] — 权重 3,815,784；原编码 `rL`；本轮新编码 `tF`
146. 内容[词] — 权重 3,815,710；原编码 `dF`；本轮新编码 `aL`
147. 它[字] — 权重 3,796,699；原编码 `sG`；本轮新编码 `zG`
148. 吗[字] — 权重 3,795,655；原编码 `eG`；本轮新编码 `qG`
149. 开[字] — 权重 3,781,673；原编码 `xJ`；本轮新编码 `vJ`
150. 信息[词] — 权重 3,743,048；原编码 `rH`；本轮新编码 `tH`
151. 国[字] — 权重 3,741,987；原编码 `zI`；本轮新编码 `xB`
152. 提高[词] — 权重 3,726,895；原编码 `sE`；本轮新编码 `zE`
153. 投资[词] — 权重 3,717,833；原编码 `sK`；本轮新编码 `zC`
154. 环境[词] — 权重 3,676,233；原编码 `cB`；本轮新编码 `sK`
155. 车[字] — 权重 3,637,604；原编码 `eL`；本轮新编码 `qF`
156. 朋友[词] — 权重 3,606,845；原编码 `wB`；本轮新编码 `eK`
157. 生活[词] — 权重 3,596,642；原编码 `wB`；本轮新编码 `eK`
158. 分[字] — 权重 3,499,206；原编码 `rI`；本轮新编码 `tB`
159. 怎么[词] — 权重 3,384,045；原编码 `tI`；本轮新编码 `gB`
160. 情况[词] — 权重 3,369,616；原编码 `tL`；本轮新编码 `gF`
161. 东西[词] — 权重 3,256,915；原编码 `aA`；本轮新编码 `dI`
162. 真的[词] — 权重 3,255,744；原编码 `dI`；本轮新编码 `aB`
163. 行[字] — 权重 3,255,650；原编码 `rL`；本轮新编码 `tF`
164. 设计[词] — 权重 3,245,935；原编码 `wL`；本轮新编码 `eF`
165. 建议[词] — 权重 3,171,878；原编码 `xD`；本轮新编码 `vD`
166. 您[字] — 权重 3,161,764；原编码 `dH`；本轮新编码 `aH`
167. 个人[词] — 权重 3,160,814；原编码 `zL`；本轮新编码 `xF`
168. 区[字] — 权重 3,131,397；原编码 `tK`；本轮新编码 `gC`
169. 水[字] — 权重 3,115,574；原编码 `wF`；本轮新编码 `eL`
170. 全[字] — 权重 3,113,011；原编码 `tB`；本轮新编码 `gK`
171. 爱[字] — 权重 3,107,527；原编码 `bJ`；本轮新编码 `bJ`
172. 天[字] — 权重 3,087,135；原编码 `sD`；本轮新编码 `zD`
173. 而且[词] — 权重 3,085,099；原编码 `bB`；本轮新编码 `bK`
174. 那么[词] — 权重 3,076,658；原编码 `dG`；本轮新编码 `aG`
175. 处理[词] — 权重 3,038,446；原编码 `eK`；本轮新编码 `qC`
176. 关注[词] — 权重 3,032,170；原编码 `zB`；本轮新编码 `xK`
177. 对于[词] — 权重 3,018,122；原编码 `aF`；本轮新编码 `dL`
178. 管理[词] — 权重 3,002,438；原编码 `zB`；本轮新编码 `xK`
179. 名[字] — 权重 2,999,469；原编码 `eL`；本轮新编码 `qF`
180. 主要[词] — 权重 2,990,140；原编码 `dK`；本轮新编码 `aC`
181. 能力[词] — 权重 2,965,184；原编码 `dB`；本轮新编码 `aK`
182. 教育[词] — 权重 2,946,238；原编码 `xA`；本轮新编码 `vI`
183. 了解[词] — 权重 2,930,469；原编码 `fA`；本轮新编码 `cI`
184. 文化[词] — 权重 2,924,455；原编码 `gI`；本轮新编码 `wB`
185. 健康[词] — 权重 2,812,269；原编码 `xD`；本轮新编码 `vD`
186. 无[字] — 权重 2,778,243；原编码 `gK`；本轮新编码 `wC`
187. 万[字] — 权重 2,768,197；原编码 `gH`；本轮新编码 `wH`
188. 同时[词] — 权重 2,765,425；原编码 `sA`；本轮新编码 `zI`
189. 带[字] — 权重 2,758,993；原编码 `aJ`；本轮新编码 `dJ`
190. 路[字] — 权重 2,736,742；原编码 `fK`；本轮新编码 `cC`
191. 因[字] — 权重 2,715,609；原编码 `vH`；本轮新编码 `fH`
192. 成为[词] — 权重 2,704,106；原编码 `eB`；本轮新编码 `qK`
193. 结果[词] — 权重 2,684,147；原编码 `xI`；本轮新编码 `vB`
194. 训练[词] — 权重 2,663,293；原编码 `rA`；本轮新编码 `tI`
195. 二[字] — 权重 2,662,851；原编码 `bB`；本轮新编码 `bK`
196. 重要[词] — 权重 2,634,392；原编码 `dA`；本轮新编码 `aI`
197. 面[字] — 权重 2,622,622；原编码 `eD`；本轮新编码 `qD`
198. 先[字] — 权重 2,600,431；原编码 `rD`；本轮新编码 `tD`
199. 最后[词] — 权重 2,595,527；原编码 `tF`；本轮新编码 `gL`
200. 此[字] — 权重 2,595,522；原编码 `gE`；本轮新编码 `wE`
201. 安全[词] — 权重 2,578,914；原编码 `bH`；本轮新编码 `bH`
202. 历史[词] — 权重 2,525,336；原编码 `fE`；本轮新编码 `cE`
203. 图[字] — 权重 2,520,287；原编码 `sK`；本轮新编码 `zC`
204. 走[字] — 权重 2,489,858；原编码 `tK`；本轮新编码 `gC`
205. 却[字] — 权重 2,443,927；原编码 `tG`；本轮新编码 `gG`
206. 老[字] — 权重 2,424,926；原编码 `fD`；本轮新编码 `cD`
207. 虽然[词] — 权重 2,416,808；原编码 `bF`；本轮新编码 `bL`
208. 认为[词] — 权重 2,405,360；原编码 `aI`；本轮新编码 `dB`
209. 挺[字] — 权重 2,393,869；原编码 `sL`；本轮新编码 `zF`
210. 社会[词] — 权重 2,388,104；原编码 `wL`；本轮新编码 `eF`
211. 菜[字] — 权重 2,375,182；原编码 `gJ`；本轮新编码 `wJ`
212. 两[字] — 权重 2,358,370；原编码 `fG`；本轮新编码 `cG`
213. 少[字] — 权重 2,355,655；原编码 `wD`；本轮新编码 `eD`
214. 总[字] — 权重 2,343,069；原编码 `tA`；本轮新编码 `gI`
215. 类[字] — 权重 2,326,056；原编码 `fF`；本轮新编码 `cL`
216. 注意[词] — 权重 2,320,640；原编码 `dK`；本轮新编码 `aC`
217. 真[字] — 权重 2,304,485；原编码 `dI`；本轮新编码 `aB`
218. 品牌[词] — 权重 2,291,673；原编码 `wH`；本轮新编码 `eH`
219. 号[字] — 权重 2,246,384；原编码 `cD`；本轮新编码 `sD`
220. 无法[词] — 权重 2,215,890；原编码 `gK`；本轮新编码 `wC`
221. 表示[词] — 权重 2,205,759；原编码 `qA`；本轮新编码 `rI`
222. 所有[词] — 权重 2,203,535；原编码 `bI`；本轮新编码 `bB`
223. 站[字] — 权重 2,197,202；原编码 `dH`；本轮新编码 `aH`
224. 次[字] — 权重 2,184,441；原编码 `gE`；本轮新编码 `wE`
225. 领域[词] — 权重 2,165,921；原编码 `fL`；本轮新编码 `cF`
226. 要求[词] — 权重 2,150,494；原编码 `vD`；本轮新编码 `fD`
227. 斯[字] — 权重 2,144,816；原编码 `bE`；本轮新编码 `bE`
228. 参考[词] — 权重 2,121,095；原编码 `gH`；本轮新编码 `wH`
229. 的话[词] — 权重 2,107,200；原编码 `aL`；本轮新编码 `dF`
230. 北京[词] — 权重 2,106,604；原编码 `qF`；本轮新编码 `rL`
231. 回答[词] — 权重 2,103,463；原编码 `cF`；本轮新编码 `sL`
232. 效果[词] — 权重 2,070,060；原编码 `rA`；本轮新编码 `tI`
233. 各[字] — 权重 2,055,288；原编码 `zL`；本轮新编码 `xF`
234. 来说[词] — 权重 2,050,222；原编码 `fJ`；本轮新编码 `cJ`
235. 特别[词] — 权重 2,016,053；原编码 `sL`；本轮新编码 `zF`
236. 老师[词] — 权重 2,005,881；原编码 `fD`；本轮新编码 `cD`
237. 控制[词] — 权重 2,001,174；原编码 `xA`；本轮新编码 `vI`
238. 风险[词] — 权重 1,989,103；原编码 `rB`；本轮新编码 `tK`
239. 两个[词] — 权重 1,980,377；原编码 `fG`；本轮新编码 `cG`
240. 强[字] — 权重 1,974,243；原编码 `tG`；本轮新编码 `gG`
241. 四[字] — 权重 1,969,564；原编码 `bE`；本轮新编码 `bE`
242. 钱[字] — 权重 1,958,153；原编码 `tD`；本轮新编码 `gD`
243. 旅游[词] — 权重 1,943,161；原编码 `fB`；本轮新编码 `cK`
244. 动[字] — 权重 1,936,470；原编码 `aA`；本轮新编码 `dI`
245. 马[字] — 权重 1,932,861；原编码 `eG`；本轮新编码 `qG`
246. 增加[词] — 权重 1,914,924；原编码 `tB`；本轮新编码 `gK`
247. 故事[词] — 权重 1,914,736；原编码 `zK`；本轮新编码 `xC`
248. 解决[词] — 权重 1,904,009；原编码 `xI`；本轮新编码 `vB`
249. 原因[词] — 权重 1,896,030；原编码 `vB`；本轮新编码 `fK`
250. 非[字] — 权重 1,874,768；原编码 `rF`；本轮新编码 `tL`
251. 股[字] — 权重 1,849,177；原编码 `zK`；本轮新编码 `xC`
252. 保持[词] — 权重 1,818,071；原编码 `qD`；本轮新编码 `rD`
253. 山[字] — 权重 1,774,419；原编码 `wH`；本轮新编码 `eH`
254. 越[字] — 权重 1,717,927；原编码 `vG`；本轮新编码 `fG`
255. 存在[词] — 权重 1,710,110；原编码 `gA`；本轮新编码 `wI`
256. 后来[词] — 权重 1,706,291；原编码 `cK`；本轮新编码 `sC`
257. 据[字] — 权重 1,689,091；原编码 `xK`；本轮新编码 `vC`
258. 米[字] — 权重 1,687,154；原编码 `eE`；本轮新编码 `qE`
259. 超[字] — 权重 1,679,740；原编码 `eD`；本轮新编码 `qD`
260. 林[字] — 权重 1,649,062；原编码 `fH`；本轮新编码 `cH`
261. 具体[词] — 权重 1,645,238；原编码 `xK`；本轮新编码 `vC`
262. 岁[字] — 权重 1,640,718；原编码 `bF`；本轮新编码 `bL`
263. 速度[词] — 权重 1,639,653；原编码 `bK`；本轮新编码 `bC`
264. 款[字] — 权重 1,625,648；原编码 `xB`；本轮新编码 `vK`
265. 合[字] — 权重 1,601,610；原编码 `cL`；本轮新编码 `sF`
266. 别[字] — 权重 1,595,352；原编码 `qI`；本轮新编码 `rB`
267. 台[字] — 权重 1,559,399；原编码 `sJ`；本轮新编码 `zJ`
268. 版[字] — 权重 1,547,361；原编码 `qH`；本轮新编码 `rH`
269. 曾[字] — 权重 1,533,244；原编码 `gB`；本轮新编码 `wK`
270. 率[字] — 权重 1,516,157；原编码 `fB`；本轮新编码 `cK`
271. 陈[字] — 权重 1,513,024；原编码 `eI`；本轮新编码 `qB`
272. 谁[字] — 权重 1,508,124；原编码 `wF`；本轮新编码 `eL`
273. 感[字] — 权重 1,497,221；原编码 `zH`；本轮新编码 `xH`
274. 合作[词] — 权重 1,495,852；原编码 `cL`；本轮新编码 `sF`
275. 贵[字] — 权重 1,467,205；原编码 `zF`；本轮新编码 `xL`
276. 表[字] — 权重 1,463,941；原编码 `qA`；本轮新编码 `rI`
277. 模式[词] — 权重 1,463,640；原编码 `eA`；本轮新编码 `qI`
278. 软件[词] — 权重 1,422,253；原编码 `aB`；本轮新编码 `dK`
279. 白[字] — 权重 1,418,104；原编码 `qJ`；本轮新编码 `rJ`
280. 罗[字] — 权重 1,417,500；原编码 `fI`；本轮新编码 `cB`
281. 送[字] — 权重 1,398,653；原编码 `bA`；本轮新编码 `bI`
282. 红[字] — 权重 1,395,814；原编码 `cA`；本轮新编码 `sI`
283. 测试[词] — 权重 1,392,208；原编码 `gL`；本轮新编码 `wF`
284. 并不[词] — 权重 1,370,641；原编码 `qL`；本轮新编码 `rF`
285. 北[字] — 权重 1,367,136；原编码 `qF`；本轮新编码 `rL`
286. 产[字] — 权重 1,342,788；原编码 `eH`；本轮新编码 `qH`
287. 另外[词] — 权重 1,339,944；原编码 `fL`；本轮新编码 `cF`
288. 便宜[词] — 权重 1,335,396；原编码 `wD`；本轮新编码 `eD`
289. 操作[词] — 权重 1,332,100；原编码 `gD`；本轮新编码 `wD`
290. 阿[字] — 权重 1,327,032；原编码 `bG`；本轮新编码 `bG`
291. 双[字] — 权重 1,325,968；原编码 `wJ`；本轮新编码 `eJ`
292. 刘[字] — 权重 1,324,738；原编码 `fJ`；本轮新编码 `cJ`
293. 卖[字] — 权重 1,262,673；原编码 `eJ`；本轮新编码 `qJ`
294. 光[字] — 权重 1,245,099；原编码 `zJ`；本轮新编码 `xJ`
295. 代表[词] — 权重 1,206,513；原编码 `aJ`；本轮新编码 `dJ`
296. 换[字] — 权重 1,183,333；原编码 `cB`；本轮新编码 `sK`
297. 球[字] — 权重 1,146,828；原编码 `tJ`；本轮新编码 `gJ`
298. 赛[字] — 权重 1,145,707；原编码 `bJ`；本轮新编码 `bJ`
299. 龙[字] — 权重 1,131,949；原编码 `fA`；本轮新编码 `cI`
300. 策略[词] — 权重 1,083,053；原编码 `gL`；本轮新编码 `wF`
301. 哈哈[词] — 权重 1,082,433；原编码 `cG`；本轮新编码 `sG`
302. 团队[词] — 权重 1,068,968；原编码 `sB`；本轮新编码 `zK`
303. 告诉[词] — 权重 1,065,234；原编码 `zD`；本轮新编码 `xD`
304. 状态[词] — 权重 1,051,017；原编码 `dJ`；本轮新编码 `aJ`
305. 拍[字] — 权重 1,036,645；原编码 `wJ`；本轮新编码 `eJ`
306. 持续[词] — 权重 1,019,249；原编码 `eE`；本轮新编码 `qE`
307. 装[字] — 权重 1,015,599；原编码 `dJ`；本轮新编码 `aJ`
308. 层[字] — 权重 986,511；原编码 `gB`；本轮新编码 `wK`
309. 推[字] — 权重 975,214；原编码 `sF`；本轮新编码 `zL`
310. 总之[词] — 权重 967,822；原编码 `tA`；本轮新编码 `gI`
311. 办[字] — 权重 957,930；原编码 `qH`；本轮新编码 `rH`
312. 类型[词] — 权重 948,556；原编码 `fF`；本轮新编码 `cL`
313. 色[字] — 权重 893,771；原编码 `bL`；本轮新编码 `bF`
314. 韩国[词] — 权重 871,696；原编码 `cH`；本轮新编码 `sH`
315. 团[字] — 权重 857,363；原编码 `sB`；本轮新编码 `zK`
316. 田[字] — 权重 855,481；原编码 `sD`；本轮新编码 `zD`
317. 哈[字] — 权重 841,246；原编码 `cG`；本轮新编码 `sG`
318. 兰[字] — 权重 840,120；原编码 `fH`；本轮新编码 `cH`
319. 欧洲[词] — 权重 838,664；原编码 `bK`；本轮新编码 `bC`
320. 赞[字] — 权重 830,724；原编码 `tH`；本轮新编码 `gH`
321. 奥[字] — 权重 793,935；原编码 `bD`；本轮新编码 `bD`
322. 楼[字] — 权重 744,737；原编码 `fK`；本轮新编码 `cC`
323. 百[字] — 权重 693,441；原编码 `qJ`；本轮新编码 `rJ`
324. 洛[字] — 权重 692,916；原编码 `fI`；本轮新编码 `cB`
325. 修改[词] — 权重 685,172；原编码 `rJ`；本轮新编码 `tJ`
326. 沙[字] — 权重 668,898；原编码 `wG`；本轮新编码 `eG`
327. 漂亮[词] — 权重 657,919；原编码 `wA`；本轮新编码 `eI`
328. 修[字] — 权重 642,771；原编码 `rJ`；本轮新编码 `tJ`
329. 哦[字] — 权重 634,170；原编码 `bA`；本轮新编码 `bI`
330. 铁[字] — 权重 631,359；原编码 `sI`；本轮新编码 `zB`
331. 破[字] — 权重 628,859；原编码 `wA`；本轮新编码 `eI`
332. 含[字] — 权重 626,783；原编码 `cH`；本轮新编码 `sH`
333. 俄罗斯[词] — 权重 620,531；原编码 `bL`；本轮新编码 `bF`
334. 瓦[字] — 权重 616,314；原编码 `gG`；本轮新编码 `wG`
335. 谈[字] — 权重 611,225；原编码 `sH`；本轮新编码 `zH`
336. 托[字] — 权重 590,350；原编码 `sI`；本轮新编码 `zB`
337. 啥[字] — 权重 587,764；原编码 `wG`；本轮新编码 `eG`
338. 卷[字] — 权重 574,581；原编码 `xB`；本轮新编码 `vK`
339. 亲[字] — 权重 534,569；原编码 `tH`；本轮新编码 `gH`
340. 探索[词] — 权重 517,769；原编码 `sH`；本轮新编码 `zH`
341. 红色[词] — 权重 431,679；原编码 `cA`；本轮新编码 `sI`
342. 草[字] — 权重 414,457；原编码 `gD`；本轮新编码 `wD`
343. 挖掘[词] — 权重 344,203；原编码 `gG`；本轮新编码 `wG`
344. 怪[字] — 权重 309,328；原编码 `zG`；本轮新编码 `xG`
345. 澳大利亚[词] — 权重 275,798；原编码 `bD`；本轮新编码 `bD`
346. 咖喱[词] — 权重 255,427；原编码 `zG`；本轮新编码 `xG`
347. 亏损[词] — 权重 198,525；原编码 `xF`；本轮新编码 `vL`
348. 亏[字] — 权重 164,945；原编码 `xF`；本轮新编码 `vL`
349. 崩溃[词] — 权重 161,840；原编码 `qB`；本轮新编码 `rK`
350. 泵[字] — 权重 57,844；原编码 `qB`；本轮新编码 `rK`

## 计算口径

- 字词候选来源：`scripts/data/词库-chai-ordered.txt`。
- 词按首音节归入一个“声母码元组 × 韵母码元组”；单字和词统一按权重竞争。
- A 是独立声母快捷层：每个声母组忽略韵母选权重最高的两条；入选 A 的字词从常规声韵格移除并顺次回填。
- 键位容易度来源：`scripts/prompts/键位分布目标.txt` 的前两列。
- 共选出 350 条两码记录、350 个不同字词；重复字词为 无。
- 180 格中，175 格各有 2 条记录，5 格为空（sE、fB、fJ、fL、xE）。
- 优化第一优先级：每个声母组权重最高的两条必须进入 A。
- 次级目标函数：`Σ(字词权重 × 新键位容易度 × 新指法容易度)`。
- 最低容易度保护：`gA`（我、我们）≥1.5000。
- 穷举 5544 种不同的指法档位分配；精确枚举受保护码元组的可行键位，再用重排不等式求其余声母组最优解；因此结果是约束目标下的全局最优。
- 同容易度位置不影响目标值；下方优先保留原位置，其余按文档顺序确定。

## 优化效果

- 总权重：1,974,588,643
- A 声母快捷层字词总权重：736,516,172；常规韵母组 `C`（ang、ia、ua、ve）让出 A 的前两候选位。
- 当前目标值：3,626,863,692.5
- 满足 A 最高优先级时的无保护上限：4,699,703,013.6
- 保护约束下最优值：4,699,703,013.6
- 保护代价：0.0000%
- 提升：29.5804%
- 加权平均组合容易度：1.8368 → 2.3801

## 最终声母键位

- `q`（容易度 1） ← `m、ch`（原位置 `e`）
- `w`（容易度 3） ← `c、w`（原位置 `g`）
- `e`（容易度 6） ← `p、sh`（原位置 `w`）
- `r`（容易度 3） ← `b`（原位置 `q`）
- `t`（容易度 2） ← `f、x`（原位置 `r`）
- `a`（容易度 3） ← `n、zh`（原位置 `d`）
- `s`（容易度 5） ← `h`（原位置 `c`）
- `d`（容易度 7） ← `d、r`（原位置 `a`）
- `f`（容易度 6） ← `y`（原位置 `v`）
- `g`（容易度 4） ← `z、q`（原位置 `t`）
- `z`（容易度 1） ← `t`（原位置 `s`）
- `x`（容易度 2） ← `g`（原位置 `z`）
- `c`（容易度 3） ← `l`（原位置 `f`）
- `v`（容易度 3） ← `k、j`（原位置 `x`）
- `b`（容易度 1） ← `s、0`（原位置 `b`）

旧位置到新键位：

`q→r  w→e  e→q  r→t  t→g  a→d  s→z  d→a  f→c  g→w  z→x  x→v  c→s  v→f  b→b`

## 最终韵母指法

- `A`（容易度 1） ← 每个声母组最高频两条（忽略韵母）；常规组 `C` 仅保留为底层映射
- `B`（容易度 0.5000） ← `ng、en、ie、uo`（原位置 `I`）
- `C`（容易度 0.5000） ← `ou、u`（原位置 `K`）
- `D`（容易度 0.5000） ← `ao、ian`（原位置 `D`）
- `E`（容易度 0.5000） ← `i`（原位置 `E`）
- `F`（容易度 0.5000） ← `ing、e`（原位置 `L`）
- `G`（容易度 0.1000） ← `uai、a、ue、iang`（原位置 `G`）
- `H`（容易度 0.1000） ← `an、in`（原位置 `H`）
- `I`（容易度 0.1000） ← `ong、o、iao、un`（原位置 `A`）
- `J`（容易度 0.1000） ← `ai、iu、uang`（原位置 `J`）
- `K`（容易度 0.1000） ← `iong、m、uan、er、eng、v`（原位置 `B`）
- `L`（容易度 0.1000） ← `ui、ei`（原位置 `F`）

常规韵母旧位置到新指法（A 顶层由声母快捷覆盖）：

`A→I  B→K  C→A  D→D  E→E  F→L  G→G  H→H  I→B  J→J  K→C  L→F`

## 高频字词变化

- 的[字]（92,123,018）：`aL` / 0.30 → `dA` / 7.00
- 是[字]（60,632,202）：`wE` / 1.50 → `eA` / 6.00
- 了[字]（59,562,863）：`fL` / 0.60 → `cA` / 3.00
- 在[字]（57,072,466）：`tJ` / 0.20 → `gA` / 4.00
- 和[字]（46,836,227）：`cL` / 0.30 → `sA` / 5.00
- 我[字]（41,082,993）：`gA` / 4.00 → `wA` / 3.00
- 有[字]（33,869,044）：`vK` / 0.30 → `fA` / 6.00
- 不[字]（32,331,838）：`qK` / 0.10 → `rA` / 3.00
- 一[字]（27,356,423）：`vE` / 1.50 → `fA` / 6.00
- 就[字]（26,406,172）：`xJ` / 0.20 → `vA` / 3.00
- 人[字]（22,310,236）：`aI` / 0.30 → `dA` / 7.00
- 也[字]（22,136,145）：`vL` / 0.30 → `fF` / 3.00
- 中[字]（22,094,930）：`dA` / 7.00 → `aA` / 3.00
- 很[字]（21,847,963）：`cI` / 0.30 → `sA` / 5.00
- 你[字]（20,296,787）：`dE` / 3.50 → `aA` / 3.00
- 可以[词]（20,283,603）：`xL` / 0.20 → `vA` / 3.00
- 都[字]（18,779,556）：`aK` / 0.30 → `dC` / 3.50
- 上[字]（17,672,983）：`wC` / 1.50 → `eA` / 6.00
- 会[字]（17,543,962）：`cF` / 1.50 → `sL` / 0.50
- 这[字]（16,995,923）：`dL` / 0.70 → `aF` / 1.50
- 一个[词]（16,335,779）：`vE` / 1.50 → `fE` / 3.00
- 好[字]（16,120,003）：`cD` / 1.50 → `sD` / 2.50
- 到[字]（14,948,468）：`aD` / 1.50 → `dD` / 3.50
- 对[字]（14,880,376）：`aF` / 1.50 → `dL` / 0.70
- 大[字]（14,749,051）：`aG` / 0.30 → `dG` / 0.70
- 他[字]（14,507,163）：`sG` / 0.50 → `zA` / 1.00
- 等[字]（14,146,876）：`aB` / 1.50 → `dK` / 0.70
- 要[字]（13,943,971）：`vD` / 1.50 → `fD` / 3.00
- 多[字]（13,521,988）：`aI` / 0.30 → `dB` / 3.50
- 还[字]（12,829,849）：`cJ` / 0.30 → `sJ` / 0.50

> 注意：加权总和最优不等于每个高频字词都变容易；码元组必须整体移动，局部退化是全局耦合的结果。
